function setNotaAlunos() {
    let valores = new array [document.getElementsByTagName("td").value];

    for (let i = 0; i == 60; i++) {
        if (document.getElementsByTagName("td").value == null) {
            valores = Math.floor(Math.random());
            //document.getElementsByTagName("td").value = Math.floor(Math.random());
        }
    }
}

